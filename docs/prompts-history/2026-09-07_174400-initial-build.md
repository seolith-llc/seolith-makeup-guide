# Prompt 2026-09-07T21:44:00Z

- Session: initial build (recorded by hand; the `UserPromptSubmit` / `Stop` hooks in `.claude/settings.json` record every later prompt automatically)
- Saved by: Claude Code

## Request

Initialize Git. Save every request prompt and response in a date time stamped markdown file inside prompts-history folder inside docs folder. Research about Make Up and instructions of how to put it and how someone would look like before and after. This would be a PWA app and both mobile friendly and working both in horizontal and verticle mode. It should work completely offline. There should be a admin mode which will show the telemetry of how the app is being used with anonymoized data. There should be a feedback button to request features, suggest bugs and corrections. There should be a guide to estimate how much time it would like. A list of products and their ratings and an affiliate links to order from outside the app. Decide on a good name. Thoroughly think about the implications and be compliant, have a good terms and conditions and be secure, share and invite friends, keep track of best way to use.

## Response

Built **Blendwise**, an offline-first makeup guide PWA, and committed it to a new git repository.

**Setup**
- `git init` on `main`; `.gitignore`; `package.json` with `start`, `test`, `check`, `icons`, `bump` scripts (no dependencies).
- Prompt history: `scripts/prompt-history.mjs` wired to the `UserPromptSubmit` and `Stop` hooks in `.claude/settings.json`. Each prompt creates `docs/prompts-history/<date>_<time>-<slug>.md`; the Stop hook fills in the response from the transcript. Pipe-tested with a synthetic transcript.

**Research** (`docs/research.md`, with sources): order of application, what each product changes visually, timing data (YouGov survey, artist timings), safety and period-after-opening rules, 2026 product picks, Amazon Associates and FTC disclosure rules.

**Name** (`docs/naming.md`): Blendwise. GlowGuide was rejected because three published apps already use it.

**App** (`src/`, plain ES modules, strict CSP, no build step)
- 7 guided looks over a 20-step library with how-to, tips, mistakes and before/after text; guided player with per-step timer, progress and a layered SVG face that builds up as steps complete.
- Before/After page with slider, per-step toggles and four skin tones.
- Time estimator per skill level with optional-step toggles, "I have N minutes" suggestions, and a personal pace model that switches on after three timed runs.
- Product catalogue (44 items) with editorial ratings, skin-type fit, Amazon affiliate links (`rel="sponsored"`, "Paid link" label, required Associates sentence) and a placeholder tag in `config.js`.
- My Kit period-after-opening tracker; Insights (streak, time vs estimate, slow steps, next look); Tips; Feedback (feature/bug/correction, on-device, copy/share/email, optional endpoint); Share and invite with anonymous codes; Settings (profile, telemetry toggle, export, delete all, install, update).
- Admin mode: PBKDF2-hashed passphrase, dashboard of anonymised events (sessions, pages, looks started/completed, durations, product clicks, orientation, shares), feedback list, import of other devices' exports.
- Consent gate with Terms and Privacy tabs; onboarding for skill, skin type and tone.
- Portrait bottom tabs / landscape side rail; light and dark; safe-area insets; service worker precache plus stale-while-revalidate; manifest with shortcuts and generated icons.

**Compliance and security** (`docs/compliance.md`, `src/data/legal.js`): Terms and Privacy Policy drafted with placeholders for company, contact and jurisdiction; no server, opt-in local telemetry, erasure and portability; CSP without inline scripts or styles; all user text escaped; affiliate disclosure adjacent to links.

**Verification**: 13 node tests and a static check (every file precached, no inline handlers or styles, version consistency) pass. In the browser: consent, onboarding, every route, a full routine run, feedback (XSS text rendered inert), kit status, admin setup/lock/login/dashboard, service worker activation, portrait and landscape layouts.

**Open before launch**: fill placeholders in `config.js` and `legal.js`, enrol in Amazon Associates, trademark check, legal review.
