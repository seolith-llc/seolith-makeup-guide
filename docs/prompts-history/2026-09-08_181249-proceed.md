# Prompt 2026-09-08T22:12:49.843Z

- Session: `3c6e4d7e-aa3b-4b00-86e8-e9f52ea12bec`
- Saved by: scripts/prompt-history.mjs (UserPromptSubmit hook)

## Request

proceed

## Response

Proceeding with the items I can do myself: merging the redesign, filling the placeholders I know, adding per-host HSTS, a trademark check, and rotating the workstation key to a Blendwise-specific one.

---

Now waiting for that deploy and verifying HSTS and the new version on the live site.

---

Done. Here's what shipped, all verified on the live site.

**Merged and deployed**
- The redesign PR is merged and live; the branch was deleted. Two automated deploys ran (1.1.0, then 1.1.1), both passed tests, delivery and the smoke test.
- Live checks: version 1.1.1, `publicUrl` set, `Strict-Transport-Security: max-age=31536000` on every response, `Cache-Control: no-cache` still passing through for `sw.js` and the app shell.

**Placeholders filled**
- `publicUrl` is `https://blendwise.amtocsoft.com/`, so invite links are stable.
- Feedback email and legal contact are `seolith.com@gmail.com` (the address on the SEOlith LLC OVH account). Company set to SEOlith LLC. Change either in `src/js/config.js` and `src/data/legal.js` if you'd rather use a different mailbox.
- Still placeholders because I don't know them: the company address and governing jurisdiction in `legal.js`, and the Amazon affiliate tag (needs enrolment).

**Security**
- HSTS is set by nginx for this host only, deliberately without `includeSubDomains`, since the zone has other subdomains this app doesn't own. Enabling HSTS in the Cloudflare zone settings would be zone-wide, so I left that alone.
- A Blendwise-specific operator key (`~/.ssh/blendwise-ops`) is authorised on the VPS and is now the default for the manual deploy script. The shared `ovh-foxy` key still works; remove its line from the host's `authorized_keys` when you're comfortable.

**Trademark**: a web search for "Blendwise" found no company, product or registration using the name, only a generic name-idea list ([source](https://www.rontar.com/blog/fertilizer-company-name-ideas/)). That is not a USPTO search; a proper clearance search still needs to be done before you invest in the name.

**Left for you**
- Set the admin passphrase on the device you'll use for the dashboard: https://blendwise.amtocsoft.com/#/admin.
- Address and jurisdiction for the Terms, then a legal review.
- Tell me if you want the runbook moved out of the public repo.

_Completed 2026-09-08T22:15:03.537Z_
