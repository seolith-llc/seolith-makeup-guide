# Prompt 2026-09-08T20:30:27.588Z

- Session: `3c6e4d7e-aa3b-4b00-86e8-e9f52ea12bec`
- Saved by: scripts/prompt-history.mjs (UserPromptSubmit hook)

## Request

What next

## Response

Here's what I'd do next, in order of value.

**1. Decide on the redesign PR.** [PR #1](https://github.com/seolith-llc/seolith-makeup-guide/pull/1) is verified and mergeable. Say "merge it" and it goes live in about two minutes.

**2. Fill the launch placeholders.** These block a real public launch:
- `src/js/config.js`: Amazon Associates tag (needs enrolment first), a feedback email, and `publicUrl` set to `https://blendwise.amtocsoft.com` so invite links are stable.
- `src/data/legal.js`: company name, address, contact email and governing law. Then have a lawyer skim the Terms and Privacy Policy.

**3. Set an admin passphrase on the live site.** Admin mode is per-device, so open https://blendwise.amtocsoft.com/#/admin on the phone or laptop you'll use for the dashboard and set it there. Nothing is centralised.

**4. Trademark and store presence.** Run the "Blendwise" trademark search from [docs/naming.md](docs/naming.md) before you promote the name. Consider a TWA wrapper for the Play Store later; the PWA is already installable on Android and iOS from the browser.

**5. Product data quality.** The 44 ratings are editorial snapshots from mid-2026. Worth a pass in a month to refresh them and add ASIN-based links once the affiliate tag exists, so links land on the product page instead of a search.

**6. Small hardening items.**
- Enable HSTS in Cloudflare (SSL/TLS > Edge Certificates) now that HTTPS is confirmed. The nginx line is ready to uncomment too.
- Move [docs/deploy-ovh.md](docs/deploy-ovh.md) to a private ops repo if you don't want the VPS address public.
- Rotate the `ovh-foxy` workstation key into a Blendwise-specific one at some point; today it's shared with the tax-manager host access.

**7. Real-user feedback.** Share the invite link with a handful of people, then read the Feedback and Insights they export. The most useful signals will be whether the time estimates feel right and whether the illustration helps.

If you only do one thing today, it's item 1, then item 2.

_Completed 2026-09-08T20:30:49.476Z_
